<!--
Figma: 캘린더 메인 화면 (Calendar Fragment - Main Container)
설명: 캘린더 기능의 메인 컨테이너 화면 (탭 전환 기능 포함)
구성:
  - 상단 헤더: 이전/다음 달 이동 버튼 + 현재 월 표시 + 마스코트 이미지
  - 탭 레이아웃: "캘린더" / "리스트" 탭 전환
  - ViewPager2: 캘린더 그리드 뷰 / 리스트 뷰를 스와이프로 전환
용도:
  - 월별 일기 작성 현황 확인
  - 캘린더 그리드 뷰와 리스트 뷰 전환
기능:
  - 이전/다음 달 이동
  - 캘린더/리스트 탭 전환
  - 스와이프로 뷰 전환
주의: 이 파일은 컨테이너 역할만 하며, 실제 캘린더 내용은
      fragment_calendar_grid.xml과 fragment_calendar_list.xml에 구현됨
-->
<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#B8D4C8">

    <!-- 상단 헤더 -->
    <LinearLayout
        android:id="@+id/layout_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent">

        <!-- 월 선택 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/btn_prev_month"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="이전 달"
                android:src="@android:drawable/ic_menu_revert" />

            <TextView
                android:id="@+id/tv_month"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:text="01월 2026"
                android:textColor="@color/black"
                android:textSize="20sp"
                android:textStyle="bold" />

            <ImageButton
                android:id="@+id/btn_next_month"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="다음 달"
                android:src="@android:drawable/ic_menu_revert"
                android:rotation="180" />

        </LinearLayout>

        <!-- 마스코트 -->
        <ImageView
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_gravity="center"
            android:layout_marginTop="8dp"
            android:contentDescription="마스코트"
            android:src="@android:drawable/ic_dialog_info" />

    </LinearLayout>

    <!-- 탭 레이아웃 -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tab_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        app:tabTextColor="@color/gray_600"
        app:tabSelectedTextColor="@color/black"
        app:tabIndicatorColor="@color/black"
        app:layout_constraintTop_toBottomOf="@id/layout_header">

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="캘린더" />

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="리스트" />

    </com.google.android.material.tabs.TabLayout>

    <!-- ViewPager2 -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/tab_layout"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>